---
i18nReady: true
title: "Configuración de StudioCMS"
description: "Opciones para definir la configuración de StudioCMS"
sidebar:
   order: 3
---

import { FileTree, Aside } from '@astrojs/starlight/components';
import ReadMore from '~/components/ReadMore.astro';

El archivo de configuración de StudioCMS se utiliza para configurar varios aspectos de StudioCMS en tu proyecto Astro. Permite definir configuraciones como la configuración de la base de datos, plugins, registro de componentes, localización y banderas de funcionalidad.

<Aside>
El archivo `studiocms.config.*`, al igual que el archivo de configuración de Astro, soporta las siguientes extensiones: `.cjs`, `.js`, `.mjs`, `.cts`, `.ts`, `.mts`. El siguiente ejemplo mostrará el uso de la extensión de archivo `.mjs`.
</Aside>

### Estructura de archivos de ejemplo

<FileTree>

- .env
- astro.config.mjs
- **studiocms.config.mjs**
- package.json
- src
  - ...

</FileTree>

### Ejemplos de archivos de configuración

```ts twoslash title="astro.config.mjs"
import node from '@astrojs/node';
import studioCMS from 'studiocms';
import { defineConfig } from 'astro/config';

export default defineConfig({
    site: 'https://demo.studiocms.dev/',
    output: 'server',
    adapter: node({ mode: "standalone" }),
    integrations: [ studioCMS() ],
});
```

```ts twoslash title="studiocms.config.mjs"
import { defineStudioCMSConfig } from "studiocms/config";

export default defineStudioCMSConfig({
    dbStartPage: false,
    // otras opciones de configuración
})
```

### Lecturas complementarias

<ReadMore>
Para más información sobre la configuración de StudioCMS, consulte la [página de referencia][reference-page].
</ReadMore>

{/* Links */}
[reference-page]: /es/config-reference/
