---
i18nReady: true
title: Sobreescrituras
description: Página de referencia para las sobreescrituras de StudioCMSOptions
sidebar:
  order: 2
---

import ReadMore from '~/components/ReadMore.astro';

Referencia del esquema de opciones de configuración de la integración de StudioCMS

```ts twoslash title="studiocms.config.mjs"
import { defineStudioCMSConfig } from 'studiocms/config';
// ---cut---
export default defineStudioCMSConfig({
  overrides: {
    CustomImageOverride: 'src/components/CustomImage.astro',
    FormattedDateOverride: 'src/components/FormattedDate.astro',
  },
});
```

## `CustomImageOverride`

`CustomImageOverride` es una cadena que se utiliza para determinar la ruta al componente de imagen personalizado.

- **Tipo:** `string`
- **Por defecto:** `undefined`

### Uso

```ts twoslash {3} title="studiocms.config.mjs"
import { defineStudioCMSConfig } from 'studiocms/config';
// ---cut---
export default defineStudioCMSConfig({
  overrides: {
    CustomImageOverride: 'src/components/CustomImage.astro',
  },
})
```

## `FormattedDateOverride`

`FormattedDateOverride` es una cadena que se utiliza para determinar la ruta al componente de fecha personalizado.

- **Tipo:** `string`
- **Por defecto:** `undefined`

### Uso

```ts twoslash {3} title="studiocms.config.mjs"
import { defineStudioCMSConfig } from 'studiocms/config';
// ---cut---
export default defineStudioCMSConfig({
  overrides: {
    FormattedDateOverride: 'src/components/FormattedDate.astro',
  },
})
```

### Componente predeterminado

```astro title="FormattedDate.astro"
---
import config from 'studiocms:config';

interface Props {
	date: Date;
}

const datetime = Astro.props.date.toISOString();
const formattedDate = Astro.props.date.toLocaleDateString(config.dateLocale, config.dateTimeFormat);
---

<time {datetime}>{formattedDate}</time>
```