---
i18nReady: true
title: "Le SDK"
description: "Découvrez le SDK de StudioCMS et comment l’utiliser."
sidebar:
   order: 3
   badge:
     text: Mis à jour
     variant: success
---

import ReadMore from '~/components/ReadMore.astro';

Le SDK de StudioCMS est un outil puissant qui vous permet d’interagir avec StudioCMS par programmation. Il fournit un ensemble de fonctions et d’utilitaires pour gérer et diffuser votre contenu via Astro DB. Il constitue également la base du tableau de bord StudioCMS et est basé sur [Effect](https://effect.website).

## Utilisation de base

Le SDK de StudioCMS est disponible sous forme de module virtuel dans votre projet Astro. Vous pouvez l’importer et l’utiliser avec la syntaxe suivante :

```ts twoslash title="example.ts" {"Working with StudioCMS SDK and Effect":4} {"Working with StudioCMS SDK without Effect":16}
/// <reference types="studiocms/v/types" />
// ---cut---
import { SDKCore, SDKCoreJs, runSDK } from 'studiocms:sdk';
import { Effect } from 'studiocms/effect';


// Créer l’Effect utilisable
const pagesEffect = Effect.gen(function* () {
  const sdk = yield* SDKCore;

 // alors faire quelque chose avec le SDK
 return yield* sdk.GET.pages()
});

// Convertir l’Effect en un programme JS/TS
const pagesResult1 = await runSDK(pagesEffect);


// Exécutez simplement le SDKCoreJS avec le wrapper runSDK !
const pagesResult2 = await runSDK(SDKCoreJs.GET.pages());
```

## Utilitaires disponibles

Voici une liste des principaux utilitaires fournis par le SDK de StudioCMS pour interagir avec la base de données.

```ts twoslash title="utils.ts" 
/// <reference types="studiocms/v/types" />
// ---cut---
import { SDKCoreJs } from 'studiocms:sdk';

const {
   db,
   dbService,
   CLEAR,
   DELETE,
   INIT,
   AUTH,
   REST_API,
   GET,
   POST,
   UPDATE,
} = SDKCoreJs;
```
